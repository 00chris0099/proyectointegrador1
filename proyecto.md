Dominio del Negocio: Mini-ERP para I.E.P. La Asunción.
Arquitectura Base: Clean Architecture, 12-Factor App, Bounded Contexts (Documental, Tesorería, Identidad)[cite: 3, 5].
Stack Tecnológico: Node.js/Express (Backend), Microsoft SQL Server (Base de Datos Relacional), Flutter (Frontend Móvil/Web Híbrido), n8n (Orquestación de Flujos).
Infraestructura IA: Vercel AI SDK / Flutter AI Toolkit para streaming de UI, Model Context Protocol (MCP) para integración de agentes.  ÉPICA 1: MÓDULO DE AUTOGESTIÓN Y FLUJO DOCUMENTALHU-01: Inicio de sesión basado en roles  API & Seguridad: Implementar API Gateway con patrón Phantom Token. Desarrollar endpoint POST /auth/login que valide credenciales en SQL Server y devuelva un token opaco al cliente, mientras el Gateway propaga un JWT con claims de roles (Apoderado, Secretaría, Dirección) a los microservicios internos.  Frontend (Flutter): Crear interfaz de autenticación aplicando controles OWASP M1 (cero credenciales hardcodeadas) y M5 (fijación de certificados HTTPS).  Base de Datos: Diseño normalizado (3FN) para la tabla de usuarios y control de acceso (RBAC).  HU-02 & HU-03: Recuperación de contraseña y Gestión de perfil  Backend: Construir endpoints POST /auth/recover y PATCH /users/profile.Seguridad: Aplicar cifrado AES/RSA para los tokens de recuperación temporales (OWASP M10).  Frontend: Formularios interactivos con validación estricta de entrada para mitigar inyecciones XSS (OWASP M4).  HU-04: Vinculación Alumno-Apoderado  Backend: Implementar el Caso de Uso GetAlumnosByApoderado aislado en la capa de Dominio, interactuando con un Repository que ejecute sentencias SELECT con JOIN en SQL Server.  Arquitectura de Agentes: Configurar un SKILL.md (Agent Skills) para que el Agente Orquestador comprenda la relación semántica 1:N entre el Apoderado y los Alumnos al momento de responder consultas automatizadas.  HU-05 & HU-06: Creación de solicitud y Carga de adjuntos  Backend: Implementar endpoint REST POST /tramites con soporte multipart/form-data. Configurar limitación estricta a 5MB y formatos PDF/JPG.  Infraestructura: Desplegar almacenamiento en la nube (S3 o equivalente) como recurso adjunto (stateless process) según The Twelve-Factor App.  Integración IA (MCP): Configurar un servidor MCP con una herramienta extractDocumentMetadata. Cuando ingrese un trámite, un Agente Especialista extrae y valida la información del PDF adjunto antes de insertarlo en la cola de revisión.  HU-07: Consulta de estado de trámite  Backend: Aplicar el patrón CQRS para separar esta consulta masiva de lectura de las operaciones transaccionales de escritura.  Frontend (Flutter): Implementar Server-Sent Events (SSE) para actualizar reactivamente la tabla del usuario final sin requerir recargas manuales.  HU-08, 09, 10, 11: Workflow de Trámites (Bandejas de Secretaría y Dirección)  Arquitectura de Dominio: Diseñar una máquina de estados (Pendiente, Observado, Derivado a Dirección, Finalizado). Los cambios de estado se ejecutarán mediante endpoints HTTP PATCH para garantizar la idempotencia.  Automatización: Encapsular la lógica de derivación en un plugin (plugin.json y hooks.json) donde eventos del ciclo de vida del trámite generen notificaciones inter-roles.  Frontend: Desarrollar dashboards particionados que garanticen tiempos de carga inferiores a 3 segundos.  HU-12: Historial de auditoría documental  Backend & DB: Aplicar el patrón Event Sourcing. Cada mutación de estado genera un registro inmutable (INSERT-only) en una tabla de auditoría en SQL Server.  ÉPICA 2: TESORERÍA, MOROSIDAD Y AUTOMATIZACIÓN  HU-13: Visualización del estado de cuenta consolidado  Backend: Desarrollar lógica de negocio CalculateDebtUseCase que consolide saldos estrictamente expresados en Soles (S/).DB: Garantizar integridad referencial mediante Foreign Keys (FK) para impedir estados financieros vinculados a alumnos no matriculados.  HU-14 & HU-16: Subida de comprobante y Conciliación de pagos  Integración IA & HITL: Diseñar un flujo donde un Agente Especialista de visión lea el voucher subido mediante un tool call. Implementar obligatoriamente un Human-in-the-Loop (HITL); el Agente extrae los montos y datos, interrumpe el flujo y el Tesorero aprueba ("Validar") o deniega ("Rechazar") la conciliación contable final en el dashboard.  Base de Datos: Transacciones financieras controladas bajo propiedades ACID estrictas en SQL Server (cero borrados físicos, solo lógicos).  HU-15 & HU-21: Dashboard de Tesorería y Reportes PDF  Backend: Implementar filtros combinados (Nivel y Aula) optimizando índices en SQL Server.Reportes: Construir un endpoint GET /reports/financial que retorne un stream binario con el archivo PDF renderizado, priorizando análisis sobre el nivel secundaria.  HU-17: Cálculo y actualización automática de morosidad  Base de Datos: Codificar un Stored Procedure (DDL/DML) programado para ejecución tipo CRON a las 00:00 horas, el cual compare la fecha_vencimiento cruzada con la fecha_actual para mutar los campos dias_mora y estado a "Moroso".  HU-18, HU-19 & HU-22: Alertas automáticas y Webhooks (n8n)  Orquestación API: Desarrollar endpoints receptores robustos y configurar Webhooks de salida hacia el motor n8n.Protección de Red: Implementar patrón de Cortocircuitos (Circuit Breakers) y reintentos automáticos garantizando que las cargas JSON se procesen aunque la API de mensajería (WhatsApp) sufra latencia (Límite RNF: 2000 ms).  Prompting / IA: Configurar el agente generador de notificaciones mediante el framework COSTAR (Tono amable a los 5 días antes; Tono asertivo y directo 3 días después de vencido), incluyendo siempre el saldo exacto en S/.  HU-20 & HU-24: Log de notificaciones y Auditoría financiera  DB: Crear las tablas inmutables logs_notificaciones y auditoria_financiera. Todo UPDATE a montos de deuda debe registrar Usuario, Fecha, Monto Original y Motivo.  HU-23: Configuración del mensaje de alerta  Arquitectura de Plantillas: Desacoplar los prompts del código base. Utilizar archivos .prompt dinámicos permitiendo a la administración inyectar variables de interpolación (ej. {nombre_apoderado}) sin redesplegar compilados. Todo expuesto tras el API Gateway protegido por Rate Limiting.  

🗄️ Capa de Persistencia (Base de Datos: MySQL / SQL Server)Para asegurar que el sistema valide las credenciales contra la base de datos de manera eficiente, se deben ejecutar las siguientes tareas:  Tarea 1.1: Diseñar y crear las tablas de Usuarios, Roles y la tabla pivote de asignación de roles. Esto debe cumplir estrictamente con el proceso de normalización hasta la Tercera Forma Normal (3FN) para mantener la integridad referencial.  Tarea 1.2: Crear un script de migración inicial (seed) para poblar la tabla de roles básicos de la institución (Apoderado, Secretaria, Dirección, Tesorería) y un usuario administrador por defecto.⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Esta capa centraliza la autenticación (RBAC) y la gestión de peticiones.  Tarea 1.3: Crear el endpoint de autenticación POST /api/auth/login configurado en Express para recibir el usuario (email) y la contraseña.  Tarea 1.4: Implementar un middleware o servicio de encriptación (ej. Bcrypt) que reciba la contraseña plana del cliente y la compare con el hash almacenado, cumpliendo con el requerimiento de que las contraseñas se almacenen encriptadas.  Tarea 1.5: Integrar la generación de un token de seguridad (ej. JWT) tras la validación exitosa. Este token debe contener el rol del usuario como claim y debe estar configurado con una expiración estricta de 12 horas.  Tarea 1.6: Configurar el manejo de excepciones HTTP en el backend para retornar códigos de estado adecuados (ej. 401 Unauthorized) y una respuesta JSON estructurada cuando las credenciales enviadas sean inválidas.  💻 Capa de Presentación (Frontend: HTML5, CSS3 y JavaScript)La interfaz debe estar diseñada para ser multiplataforma y adaptable.  Tarea 1.7: Maquetar el formulario de inicio de sesión utilizando HTML5 y frameworks CSS (Bootstrap o Tailwind). El diseño debe incluir los campos de usuario (email) y contraseña.  Tarea 1.8: Aplicar principios de diseño "Mobile First", asegurando que el portal sea completamente Responsive y priorizando su correcta visualización en smartphones.  Tarea 1.9: Implementar validaciones del lado del cliente mediante JavaScript para asegurar que el formato del correo sea correcto y los campos no estén vacíos antes de ejecutar la petición a la API.Tarea 1.10: Integrar el consumo del endpoint /login con Fetch API o Axios. En caso de error, el frontend debe capturar la respuesta y mostrar una alerta o notificación visual indicando que las credenciales son inválidas.  Tarea 1.11: Guardar el token seguro emitido por el backend (en localStorage o cookies seguras).Tarea 1.12: Desarrollar la lógica de enrutamiento (Router) que descodifique el rol del usuario validado y lo redirija automáticamente al Dashboard correspondiente (ej. panel de apoderados, secretaría o dirección).  🔒 Pruebas y Seguridad (QA)Tarea 1.13: Escribir pruebas unitarias y de integración para asegurar el control de acceso, confirmando que usuarios no autenticados o con roles incorrectos no puedan acceder a vistas protegidas, garantizando así la privacidad de los datos.  


Para la HU-02: Recuperación de contraseña, el objetivo principal es permitir que cualquier usuario solicite el restablecimiento de su acceso mediante su correo electrónico registrado, reduciendo la necesidad de soporte técnico manual.  Siguiendo el estándar de arquitectura limpia y desarrollo ágil planteado para el Mini-ERP, aquí tienes el desglose completo de tareas granulares divididas por capas técnicas para su correcta implementación:  🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 2.1: Modificar la tabla Usuarios en SQL Server para añadir las columnas necesarias que den soporte al flujo de recuperación temporal: token_recuperacion (VARCHAR), fecha_expiracion_token (DATETIME) y token_usado (BIT/BOOLEAN).  Tarea 2.2: Crear una consulta indexada o procedimiento almacenado que busque eficientemente si el correo electrónico ingresado existe en el sistema y, de ser así, actualice el registro insertando el token generado y su tiempo de expiración de manera atómica.  ⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Tarea 2.3: Desarrollar el endpoint POST /api/auth/recover encargado de recibir la solicitud inicial con el correo electrónico del usuario.  Tarea 2.4: Implementar la lógica de verificación del correo contra la base de datos. Nota de seguridad: Si el correo no existe, el backend debe retornar una respuesta genérica de éxito simulado (evitando la enumeración de usuarios para mitigar riesgos de seguridad).  Tarea 2.5: Programar el módulo de generación de tokens criptográficos temporales únicos (utilizando librerías nativas como crypto para generar un hash seguro o un JWT firmado) con un tiempo de vida corto y estricto (ej. 15 a 30 minutos).  Tarea 2.6: Configurar un microservicio de envío de correos (utilizando NodeMailer, un servicio SMTP transaccional o disparando un Webhook asíncrono hacia n8n) para despachar la plantilla HTML del mensaje que contenga el enlace dinámico y el token de recuperación seguro al correo del solicitante.  Tarea 2.7: Desarrollar el endpoint POST /api/auth/reset-password que reciba el token enviado por URL y la nueva contraseña. Debe validar que el token sea verídico, que no haya expirado y que no haya sido usado previamente.  Tarea 2.8: Integrar el proceso de encriptación hash (ej. Bcrypt) para cifrar la nueva contraseña provista por el usuario antes de guardarla de forma inmutable mediante un UPDATE en SQL Server, invalidando inmediatamente el token utilizado.  💻 Capa de Presentación (Frontend: Aplicación Web / Móvil)Tarea 2.9: Diseñar y maquetar la interfaz del formulario de solicitud de recuperación ("¿Olvidaste tu contraseña?"), que incluya un único campo de texto para la captura del correo electrónico.  Tarea 2.10: Aplicar controles de validación estricta de tipos y sanitización en los inputs para mitigar posibles ataques de inyección de código (XSS) antes del envío de la petición a la API.  Tarea 2.11: Desarrollar y maquetar la pantalla exclusiva de "Restablecer Contraseña", a la cual el enlace enviado por correo redirigirá al usuario. Esta interfaz debe contener los campos para escribir la nueva contraseña y su respectiva confirmación.  Tarea 2.12: Implementar lógica en JavaScript para validar en tiempo real que los campos de la nueva contraseña coincidan y cumplan con los requisitos mínimos de complejidad exigidos por la institución educativa antes de habilitar el botón de envío.Tarea 2.13: Conectar ambos formularios con los servicios HTTP del backend (Fetch o Axios) y gestionar de forma reactiva los mensajes de estado en la pantalla (ej. "Enlace enviado a su correo", "Contraseña restablecida correctamente" o errores en caso de token caducado).  🔒 Pruebas y Seguridad (QA)Tarea 2.14: Diseñar y ejecutar pruebas automatizadas (unitarias y de integración) para validar el ciclo de vida del token (asegurar que un token expire exactamente tras cumplir su tiempo límite, que quede inutilizable tras su primer uso y que no se puedan actualizar contraseñas con tokens alterados).  

Para la HU-03: Gestión de perfil y datos personales, el objetivo de negocio es garantizar que el apoderado mantenga su información de contacto (correo y número de celular) estrictamente actualizada. Esto es un habilitador crítico para la Épica 2, ya que de la precisión de esta data depende el éxito del sistema automatizado de alertas de morosidad vía n8n y WhatsApp.  Aplicando los principios de Clean Architecture, The Twelve-Factor App y seguridad Zero Trust, presento el desglose técnico de tareas para su implementación:🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 3.1: Crear procedimientos almacenados (Stored Procedures) o consultas parametrizadas optimizadas para SELECT y UPDATE de los campos correo y celular en la tabla Usuarios.  Tarea 3.2: Asegurar que la actualización de datos dispare un registro en la tabla de auditoría (Event Sourcing) para mantener un log inmutable de qué datos de contacto existían previamente y en qué fecha se modificaron.⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Tarea 3.3: Desarrollar el endpoint GET /api/users/profile para que el cliente recupere sus datos actuales. La identidad del usuario (ID) debe extraerse de forma segura desde los claims del JWT decodificado por el API Gateway (Patrón Phantom Token), evitando recibir el ID por parámetro en la URL para mitigar vulnerabilidades IDOR (Insecure Direct Object Reference).Tarea 3.4: Construir el endpoint PATCH /api/users/profile respetando los principios RESTful para permitir actualizaciones parciales del recurso.Tarea 3.5: Implementar un middleware de validación estricta de esquemas (ej. con Zod o Joi) que aplique expresiones regulares (Regex) para asegurar el formato estándar del correo electrónico y validar que el número de celular cumpla con el formato telefónico esperado (ej. 9 dígitos para Perú), mitigando inyecciones XSS o SQL.  Tarea 3.6: Orquestación de Eventos: Al confirmarse un PATCH exitoso, el backend debe emitir un evento asíncrono (Webhook) hacia n8n para actualizar instantáneamente la caché de contactos del motor de mensajería, asegurando que las próximas alertas tempranas se envíen al nuevo destino.  💻 Capa de Presentación (Frontend: Flutter / Web)Tarea 3.7: Construir la vista de "Mi Perfil" (UI) empleando un diseño Responsive. Al inicializarse (hook de ciclo de vida), la vista debe consumir el endpoint GET y poblar los campos de texto con la información actual del apoderado.  Tarea 3.8: Configurar los campos de teléfono y correo para que sean editables, aplicando restricciones a nivel de teclado en dispositivos móviles (ej. TextInputType.phone y TextInputType.emailAddress en Flutter).  Tarea 3.9: Implementar validación local (Client-side) que evalúe el formato de los datos antes de habilitar el botón de guardado y enviar el payload JSON al servidor.  Tarea 3.10: Consumir el endpoint PATCH y gestionar el estado de la promesa. Al recibir un HTTP 200 OK, renderizar una notificación efímera (Snackbar o Toast) con el mensaje exacto: "Datos actualizados exitosamente".  🤖 Integración MCP (Model Context Protocol) y AgentesTarea 3.11: Actualizar el servidor MCP central añadiendo la herramienta updateContactInfo. Si en el futuro se despliega un Agente de Soporte Conversacional, este agente podrá ejecutar un Tool Call validado para actualizar el celular del apoderado directamente desde el chat, manteniendo la estructura de datos unificada bajo los mismos Guardrails de seguridad del backend.🔒 Pruebas y QATarea 3.12: Ejecutar pruebas de integración comprobando el Principio de Mínimo Privilegio: verificar que el token de un Apoderado A sea matemáticamente incapaz de ejecutar un PATCH sobre el perfil de un Apoderado B.

🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 4.1: Modelar la relación en la base de datos aplicando integridad referencial mediante Llaves Primarias (PK) y Llaves Foráneas (FK). Se debe relacionar la entidad Apoderados con la entidad Alumnos garantizando que no existan redundancias (Tercera Forma Normal).  Tarea 4.2: Desarrollar el comando DML (SELECT con cláusulas JOIN) para consultar dinámicamente el listado de estudiantes asociados al DNI del apoderado autenticado.  ⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Tarea 4.3: Aislar la lógica en la capa de Dominio creando el caso de uso GetAlumnosByApoderado (Clean Architecture), asegurando que las reglas centrales no dependan de la base de datos ni de la interfaz web.  Tarea 4.4: Implementar el endpoint RESTful GET /api/apoderados/me/alumnos.  Tarea 4.5: Implementar delegación de accesos aplicando el patrón Phantom Token en el API Gateway; el microservicio extraerá el DNI de forma segura desde el JWT firmado internamente, evitando que el cliente envíe su propio identificador en la URL y previniendo vulnerabilidades de escalada de privilegios.  💻 Capa de Presentación (Frontend: Híbrido Web/Móvil)Tarea 4.6: Diseñar el componente visual en Flutter o la vista web Responsive para renderizar el listado. Cada tarjeta o fila debe exponer los datos básicos del alumno extraídos de la base de datos.  Tarea 4.7: Implementar lógica de validación de estado vacío (Empty State). Si la respuesta del backend es un arreglo sin elementos, la interfaz debe condicionar la renderización para mostrar el mensaje: "Contactar a secretaría para la vinculación".  Tarea 4.8: Asegurar que los datos mostrados en la pantalla móvil o web apliquen validación estricta de salida para mitigar riesgos de inyección (XSS), cumpliendo con el control M4 del OWASP Mobile Top 10.  🤖 Integración de Agentes e Inteligencia Artificial (MCP)Tarea 4.9: Configurar un Agent Skill (Habilidad de Agente). Se debe empaquetar un archivo .prompt independiente o un SKILL.md que actúe como "especificación viva". Este documento enseñará de forma diferida (revelación progresiva) al Agente Orquestador la relación semántica 1:N entre apoderados y alumnos para flujos de soporte o consultas automáticas futuras.  Tarea 4.10: Añadir una herramienta de solo lectura (getLinkedStudents) en el servidor MCP. Para cumplir con el Principio de Mínimo Privilegio, este agente actuará estrictamente bajo el contexto del usuario autenticado (OAuth/JWT de esa sesión específica), bloqueando matemáticamente el acceso a la base de datos transaccional completa.  🔒 Auditoría y DevOpsTarea 4.11: Escribir pruebas automatizadas (Feedback en el flujo CI/CD) para garantizar la separación de inquilinos (tenants). Se debe certificar de manera irrefutable que el token del "Apoderado A" jamás podrá recuperar el registro de alumnos asociados al "Apoderado B".  

Para la HU-05: Creación de nueva solicitud de trámite , el objetivo principal es que el apoderado pueda iniciar gestiones administrativas de forma virtual seleccionando opciones preconfiguradas, lo cual reducirá los tiempos de espera y la carga en la secretaría.  Al tratarse de la entrada principal del flujo documental de tu Mini-ERP, aquí tienes el desglose estructurado de tareas para su implementación:🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 5.1: Diseñar y crear una tabla de dominio/catálogo llamada Tipos_Tramite (ej. "Constancia de vacante", "Justificación de inasistencia", etc.) para alimentar la lista preconfigurada del sistema de manera dinámica.  Tarea 5.2: Crear la tabla transaccional Tramites aplicando integridad referencial. Debe contener Claves Foráneas (FK) vinculando al Apoderado (creador del trámite), al Alumno implicado y al Tipo_Tramite.Tarea 5.3: Configurar restricciones en la base de datos: el campo estado debe tener el valor predeterminado estricto de "Pendiente" al momento de la inserción , el campo de fecha y hora debe capturarse usando la función del motor de base de datos (ej. GETDATE()), y se debe asignar una columna id_seguimiento que garantice unicidad.  ⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Tarea 5.4: Desarrollar el endpoint GET /api/tramites/tipos que retorne al cliente el catálogo activo de tipos de trámite preconfigurados.  Tarea 5.5: Construir el endpoint principal POST /api/tramites para procesar el ingreso de la solicitud. La identidad del apoderado creador debe extraerse directamente del token de seguridad (JWT) y no del cuerpo de la petición.Tarea 5.6: Implementar lógica en el servidor para generar el ID de seguimiento único y amigable para el apoderado (por ejemplo, utilizando un prefijo estructurado como TRM-2026-ABCD) antes de registrarlo.  Tarea 5.7: Asegurar que la fecha y hora del registro se capturen desde el servidor de backend (o la base de datos) automáticamente para evitar manipulaciones si el dispositivo del cliente tiene la hora alterada.  💻 Capa de Presentación (Frontend: Híbrido Web/Móvil con Flutter)Tarea 5.8: Construir la interfaz (formulario) de "Nuevo Trámite". Al iniciar el componente, debe consumir el endpoint GET de tipos de trámite y renderizar un menú desplegable (Select) para que el apoderado elija la gestión deseada.  Tarea 5.9: Incluir un componente selector (alimentado por la HU-04) para que el usuario indique a qué hijo matriculado corresponde la solicitud.Tarea 5.10: Integrar el consumo del endpoint POST. Al recibir una respuesta exitosa (HTTP 201 Created), la UI debe desplegar un modal o pantalla de confirmación que resalte claramente el ID de seguimiento único generado para el usuario.  🔒 Pruebas de Seguridad y QATarea 5.11: Prevención de Inyección de Estados: Ejecutar pruebas que verifiquen que, si un usuario malintencionado manipula el JSON de la petición e intenta enviar la solicitud con un estado superior (ej. {"estado": "Finalizado"}), el backend lo ignore y sobrescriba el estado invariablemente a "Pendiente".  Tarea 5.12: Protección contra duplicidad: Implementar técnicas de Debounce en el botón de guardado del frontend y validación de limitación de tasa (Rate Limiting) en el backend para evitar que doble clics generen trámites repetidos.
¡Perfecto! Vamos con la HU-06: Carga de documentos adjuntos. Esta historia es fundamental porque materializa el objetivo de eliminar el uso de expedientes físicos y digitalizar los sustentos de las solicitudes (ej. certificados médicos, recibos, etc.).  Basado en la arquitectura definida para el Mini-ERP (12-Factor App, Clean Architecture y despliegue en la nube), el procesamiento de archivos debe ser manejado como un recurso externo (stateless). Aquí tienes el desglose técnico de tareas:☁️ Infraestructura y Almacenamiento (Cloud)Tarea 6.1: Crear y configurar un Bucket de almacenamiento en la nube (ej. Amazon S3, Google Cloud Storage o equivalente) para el alojamiento de archivos estáticos.Tarea 6.2: Configurar las políticas de CORS (Cross-Origin Resource Sharing) y los roles IAM (Identity and Access Management) para que el backend pueda escribir archivos de forma segura y restringida, evitando que los documentos sean de acceso público sin autorización.🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 6.3: Crear una tabla transaccional Documentos_Adjuntos (o modificar la tabla Tramites si la relación será estrictamente 1:1) que incluya llaves foráneas hacia el id_tramite.Tarea 6.4: Añadir campos para registrar metadatos críticos del archivo: url_archivo (ruta en S3), nombre_original, tipo_mime (PDF o JPG) y peso_bytes.⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Tarea 6.5: Configurar un middleware en Express (como multer) que sea capaz de procesar peticiones con el formato multipart/form-data.Tarea 6.6: Implementar una validación estricta a nivel de servidor que aborte la petición inmediatamente si el archivo supera los 5MB de peso máximo permitido.  Tarea 6.7: Validar las extensiones permitidas, restringiendo el ingreso única y exclusivamente a formatos PDF y JPG mediante la lectura del MIME type real (Magic Numbers), no solo confiando en la extensión del nombre del archivo.  Tarea 6.8: Desarrollar el script de carga (Upload Service) que envíe el buffer del archivo hacia el Bucket de S3 y retorne la URL definitiva.  Tarea 6.9: Registrar en la base de datos la URL del archivo y vincularla al ID de la solicitud del trámite creado previamente en la HU-05.  💻 Capa de Presentación (Frontend: Híbrido Web/Móvil con Flutter)Tarea 6.10: Integrar un componente de selección de archivos (File Picker UI) que permita al usuario abrir la galería de su dispositivo móvil o el explorador de archivos en la web.Tarea 6.11: Implementar una validación del lado del cliente (Client-side) que bloquee la selección de archivos mayores a 5MB y formatos incorrectos antes de iniciar el consumo de red, mejorando la experiencia del usuario.Tarea 6.12: Integrar el consumo del endpoint de carga capturando el evento de subida. Se debe renderizar una barra de progreso visual en la interfaz utilizando onUploadProgress de Axios o la librería HTTP nativa de Flutter, para dar feedback en tiempo real al apoderado.  🤖 Integración IA y Model Context Protocol (MCP)Tarea 6.13: Configurar el servidor MCP con la herramienta extractDocumentMetadata.Tarea 6.14: Integrar el flujo para que un Agente Especialista (procesamiento de lenguaje natural/visión) lea y valide los metadatos y contenido del PDF adjunto, extrayendo información clave antes de insertar definitivamente el trámite en la cola de revisión de Secretaría.🔒 Pruebas de Seguridad y QATarea 6.15: Prueba de evasión de formato: Intentar subir un archivo malicioso (ej. un script .exe o .php renombrado como .pdf) para asegurar que el validador de Magic Numbers del backend bloquee el ataque.Tarea 6.16: Pruebas de latencia: Simular subidas con conexiones de red lentas (3G throttling) para asegurar que la barra de progreso del frontend responda correctamente y que el servidor no arroje errores por timeout antes de finalizar la carga.

 Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 7.1: Crear índices no agrupados (Non-Clustered Indexes) en la tabla Tramites sobre las columnas id_apoderado y fecha_creacion. Esto optimizará las consultas de solo lectura (CQRS), ya que será una tabla consultada masivamente por los padres.Tarea 7.2: Escribir la consulta DML (SELECT) o Vista de base de datos que recupere el historial uniendo (JOIN) los datos del trámite con la tabla Tipos_Tramite y Alumnos para devolver una respuesta consolidada y plana al backend.⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Tarea 7.3: Desarrollar el endpoint REST tradicional GET /api/tramites/me que recupere la lista paginada del histórico de trámites. La identidad del apoderado se debe extraer de forma segura del token JWT (API Gateway).Tarea 7.4: Implementar un endpoint para comunicación unidireccional en tiempo real mediante Server-Sent Events (SSE), por ejemplo GET /api/tramites/stream. Este canal mantendrá una conexión abierta con el cliente.Tarea 7.5: Configurar el bus de eventos en el backend: cada vez que el personal administrativo ejecute un cambio de estado en un documento (HU-08 a HU-11), el servidor debe emitir un evento con el id_tramite y el nuevo_estado hacia el canal SSE correspondiente al apoderado propietario.💻 Capa de Presentación (Frontend: Híbrido Web/Móvil con Flutter)Tarea 7.6: Maquetar la vista "Mis Trámites" mediante una tabla o lista interactiva. Debe mostrar claramente el ID de seguimiento, fecha, tipo de solicitud, nombre del alumno y el estado actual.  Tarea 7.7: Implementar un sistema de diseño con insignias (Badges) o colores semánticos para los estados: Gris/Amarillo para "Pendiente", Naranja/Rojo para "Observado", y Verde para "Finalizado".Tarea 7.8: Consumir el endpoint GET /api/tramites/me en el hook de inicialización de la pantalla para realizar la carga inicial de datos.Tarea 7.9: Implementar la suscripción al canal SSE (Server-Sent Events) o WebSockets en el cliente. Al recibir un evento de actualización, el frontend debe buscar el ID del trámite en el estado local (Store/Provider) y mutar el campo visual de manera reactiva.  🔒 Pruebas y QATarea 7.10: Prueba de Reactividad: Simular una sesión de apoderado en el navegador A y una sesión de secretaria en el navegador B. Al cambiar el estado en la sesión B, asegurar que la tabla en la sesión A se actualice dinámicamente en menos de 2 segundos sin F5.Tarea 7.11: Aislamiento de Sesión: Verificar que a través de la conexión en tiempo real (SSE), un apoderado no reciba filtraciones o notificaciones de cambios de estado de trámites que pertenecen a otros apoderados.

 Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 8.1: Crear un índice filtrado (Filtered Index) en la tabla Tramites sobre la columna estado (donde estado = 'Pendiente'). Esto es crucial para el rendimiento, ya que la secretaria consultará masivamente solo este subconjunto de datos.Tarea 8.2: Construir la consulta DML (SELECT) o un Procedimiento Almacenado que recupere la lista de trámites pendientes realizando los JOIN correspondientes con las tablas Apoderados (para mostrar quién lo envió), Alumnos (sobre quién trata) y Tipos_Tramite.⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Tarea 8.3: Desarrollar el endpoint GET /api/admin/tramites/pendientes.  Tarea 8.4: Implementar el middleware de Autorización (RBAC). El sistema debe extraer el claim del rol desde el JWT y verificar estrictamente que el usuario tenga el rol de "Secretaría" o "Administrador". Si un "Apoderado" intenta consumir este endpoint, el servidor debe rechazar la petición con un 403 Forbidden.  Tarea 8.5: Añadir lógica de filtrado dinámico en el backend. El endpoint debe aceptar parámetros de consulta (query params) como ?fecha_inicio=...&fecha_fin=...&tipo_tramite=... para delegar la carga de procesamiento (filtros) a la base de datos y no sobrecargar el cliente.  Tarea 8.6: Desarrollar el endpoint GET /api/tramites/:id/detalle que devuelva la información completa de un trámite específico, incluyendo la URL del archivo adjunto (S3) generado en la HU-06.  💻 Capa de Presentación (Frontend: Híbrido Web/Móvil con Flutter)Tarea 8.7: Maquetar el Dashboard de Secretaría. Implementar una tabla de datos interactiva (DataGrid) que liste los trámites pendientes, ordenados por defecto desde el más antiguo al más reciente (FIFO).Tarea 8.8: Construir una barra de herramientas (Toolbar) en la parte superior de la tabla con los controles de filtro: un selector de rango de fechas (Date Range Picker) y un menú desplegable para filtrar por "Tipo de Trámite".  Tarea 8.9: Desarrollar un Modal (o vista de detalle) que se abra al hacer clic en un registro. Este modal debe consumir el endpoint de detalle y mostrar un visor integrado de PDF/Imágenes para que la secretaria lea el archivo adjunto sin tener que descargarlo.  Tarea 8.10: Integrar en la fila de la tabla y en la vista de detalle los botones de acción rápida: "Observar" (rojo/naranja) y "Derivar a Dirección" (azul/verde). Estos botones prepararán el estado para las HU-09 y HU-10.  🔒 Pruebas de Calidad y Seguridad (QA)Tarea 8.11: Prueba de Rendimiento (Load Testing): Simular una base de datos con miles de trámites históricos finalizados y asegurar que la consulta de la bandeja de "Pendientes" siga respondiendo en menos de 3 segundos al utilizar los índices adecuados.Tarea 8.12: Prueba de Control de Acceso: Ejecutar tests automatizados forzando peticiones al endpoint de secretaría utilizando un token de apoderado para comprobar que el cortafuegos de roles funciona correctamente.

 🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 9.1: Configurar la consulta DML (UPDATE) transaccional que modifique la columna estado del trámite específico al valor exacto de "Derivado a Dirección".Tarea 9.2: Asegurar que el sistema registre el nombre (o ID) del usuario que realizó la derivación. Esto se logra insertando un registro inmutable en la tabla de historial/auditoría documental, capturando la fecha, hora, estado anterior y el nuevo estado.  ⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Tarea 9.3: Desarrollar el endpoint HTTP PATCH /api/tramites/:id/derivar. Se utiliza PATCH y no POST o PUT para garantizar la idempotencia de la mutación del estado, tal como lo exige la arquitectura de dominio base.Tarea 9.4: Implementar validaciones de la Máquina de Estados en la capa de negocio: el backend debe verificar que el trámite se encuentre actualmente en estado "Pendiente" o "Observado" antes de permitir la derivación. Si ya está "Finalizado", la petición debe ser rechazada.Tarea 9.5: Disparar un evento interno (hook / Webhook local). Al completarse el PATCH exitosamente, el backend debe emitir un evento a través del canal de Server-Sent Events (SSE) dirigido exclusivamente a los usuarios conectados que posean el rol de "Dirección".💻 Capa de Presentación (Frontend: Híbrido Web/Móvil con Flutter)Tarea 9.6: Conectar el botón rápido "Derivar a Dirección" (maquetado en la HU-08) para que consuma el endpoint PATCH. Se debe implementar un cuadro de diálogo (Modal de confirmación) para evitar toques accidentales por parte de la secretaria.Tarea 9.7: Manejo reactivo del estado local (State Management). Al recibir un 200 OK del backend, el sistema debe eliminar inmediatamente el trámite de la bandeja de Secretaría  sin requerir que el usuario presione F5 para recargar la página.  Tarea 9.8: Desarrollar el componente de notificación interna visual en el panel de Dirección. Cuando el frontend de la directora reciba el evento SSE, debe mostrar un indicador (ej. un punto rojo o un toast) alertando que un nuevo documento ha sido insertado en su bandeja.  🔒 Pruebas de Calidad y Seguridad (QA)Tarea 9.9: Prueba de Flujo de Bandejas: Escribir un test de integración (E2E) que valide que, al ejecutar la acción, el trámite desaparezca efectivamente de la vista de Secretaría y se mueva a la bandeja de Dirección.  Tarea 9.10: Validación de Auditoría: Comprobar en la base de datos que la acción guardó correctamente el nombre del usuario de secretaría responsable de la derivación.  

 Para la HU-10: Observación de trámites , el objetivo principal es que la secretaria pueda marcar un trámite con el estado "Observado" y adjuntar obligatoriamente un comentario. Esta funcionalidad evita el flujo innecesario de documentos incompletos hacia la Dirección, bloqueando su aprobación hasta que el apoderado subsane el error.  Como tu AGENTE FULLSTACK, he diseñado el desglose de tareas garantizando el estricto cumplimiento de los criterios de aceptación y la máquina de estados del Mini-ERP:🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 10.1: Modificar la estructura de la tabla Tramites añadiendo una columna comentario_observacion (tipo NVARCHAR o TEXT) para almacenar de forma persistente el motivo del rechazo parcial. Alternativamente, si se requiere un historial, crear una tabla Observaciones_Tramite relacionada mediante Llave Foránea (FK).Tarea 10.2: Configurar la consulta DML (UPDATE) que modifique simultáneamente el estado a "Observado" y registre el texto del comentario.Tarea 10.3: Asegurar que esta modificación dispare un registro en la tabla de auditoría documental (Event Sourcing) capturando el cambio de estado y el usuario administrativo responsable.⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Tarea 10.4: Desarrollar el endpoint HTTP PATCH /api/tramites/:id/observar.Tarea 10.5: Implementar un middleware de validación (ej. con Zod o Joi) que obligue estrictamente a que el cuerpo de la petición (body) contenga el campo motivo o comentario. Si este campo está vacío o no existe, el servidor debe rechazar la solicitud con un error 400 Bad Request.  Tarea 10.6: Integrar la regla de validación de la Máquina de Estados: El backend debe asegurar que un trámite solo pueda ser observado si su estado actual es "Pendiente". Además, debe bloquear lógicamente el trámite para que no pueda ser derivado a Dirección ni aprobado mientras mantenga el estado "Observado".  Tarea 10.7: Disparar un evento de notificación a través del canal Server-Sent Events (SSE) dirigido específicamente al ID del apoderado propietario del trámite, informándole en tiempo real que su solicitud requiere atención.  💻 Capa de Presentación (Frontend: Híbrido Web/Móvil con Flutter)Tarea 10.8: Maquetar un Modal o cuadro de diálogo emergente que se active al hacer clic en el botón "Observar" (previamente implementado en el dashboard de Secretaría). Este modal debe contener un área de texto (TextField o textarea) para redactar el motivo.  Tarea 10.9: Implementar validación del lado del cliente (Client-side) que mantenga el botón de "Guardar Observación" deshabilitado (estado disabled) hasta que la secretaria haya ingresado texto válido en el área de comentarios.  Tarea 10.10: Actualizar la vista del Portal de Apoderados (HU-07). Cuando el padre reciba el aviso de atención, la interfaz debe mostrar visualmente el trámite en estado "Observado" (ej. con un badge de color naranja o rojo) y desplegar un recuadro de alerta con el texto del comentario redactado por la secretaria.  🔒 Pruebas de Calidad y Seguridad (QA)Tarea 10.11: Pruebas de Validación de Reglas de Negocio: Ejecutar tests automatizados forzando una petición PATCH al endpoint de observar sin enviar comentario, verificando que el servidor bloquee la acción y cumpla con el criterio de obligatoriedad.  Tarea 10.12: Pruebas de Bloqueo de Aprobación: Simular un intento de consumición del endpoint de aprobación de Dirección sobre un trámite "Observado" para asegurar que la máquina de estados rechace el flujo de forma efectiva.  

 Para la HU-11: Aprobación final de documentos, llegamos al cierre del ciclo operativo del Módulo Documental. El objetivo es que la Directora valide y marque un trámite como "Finalizado", bloqueando futuras ediciones, registrando la fecha de culminación y notificando al apoderado. Además, conectando esto con el Requerimiento Funcional 10 (RF-10), esta acción debe generar la constancia final.  Como tu AGENTE FULLSTACK, he estructurado las tareas necesarias para construir el entorno de la Dirección y cerrar la máquina de estados del ERP de forma segura:🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 11.1: Modificar la tabla Tramites (si no se hizo previamente) para asegurar que exista una columna fecha_culminacion (tipo DATETIME, permitiendo valores nulos inicialmente).Tarea 11.2: Construir la consulta DML (SELECT) específica para la Dirección, que filtre y retorne estrictamente los trámites donde estado = 'Derivado a Dirección'.  Tarea 11.3: Crear la consulta transaccional (UPDATE) que cambie el estado a "Finalizado", asigne la fecha y hora actual del servidor a fecha_culminacion, y dispare el guardado en la tabla de auditoría documental (HU-12).  ⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Tarea 11.4: Desarrollar el endpoint GET /api/direccion/tramites/derivados. Implementar un estricto control de acceso basado en roles (RBAC) verificando el JWT para que únicamente un usuario con rol "Dirección" o "Administrador" pueda consumir esta lista.Tarea 11.5: Desarrollar el endpoint HTTP PATCH /api/tramites/:id/aprobar para ejecutar la aprobación final.Tarea 11.6: Implementar el Bloqueo Definitivo (Máquina de Estados): Modificar los middlewares de todos los endpoints de escritura (PATCH, PUT, DELETE) vinculados a los trámites para que, si el estado actual es "Finalizado", el servidor retorne inmediatamente un 403 Forbidden (inmutabilidad del documento).  Tarea 11.7: Desarrollar un servicio de renderizado de documentos (ej. con Puppeteer o PDFKit) que tome los datos del trámite aprobado y genere la constancia oficial en formato PDF, subiéndola al bucket de S3.  Tarea 11.8: Emitir el evento de notificación final (vía Server-Sent Events o un webhook hacia el correo del apoderado) para informarle que su gestión ha concluido exitosamente.  💻 Capa de Presentación (Frontend: Híbrido Web/Móvil con Flutter)Tarea 11.9: Maquetar el Dashboard de Dirección. Esta interfaz debe ser limpia y mostrar una tabla de datos interactiva que liste únicamente los trámites derivados por la secretaría.  Tarea 11.10: Integrar un modal de revisión rápida donde la directora pueda leer el historial del documento, ver los adjuntos y los comentarios previos dejados por la secretaria.Tarea 11.11: Implementar el botón de acción principal "Aprobar / Finalizar Trámite". Al pulsarlo, se debe mostrar un diálogo de confirmación estricto indicando que la acción es irreversible.Tarea 11.12: Actualizar la vista "Mis Trámites" del apoderado (HU-07). Al recibir la notificación de finalización, la interfaz debe mostrar el trámite en estado "Finalizado" (color verde) y habilitar un botón de descarga para que el padre obtenga su constancia en PDF.
🔒 Pruebas de Calidad y Seguridad (QA)Tarea 11.13: Prueba de Inmutabilidad: Forzar desde el cliente (Postman) un intento de "Observar" o "Derivar" un trámite que ya tiene el estado "Finalizado", validando que el servidor bloquee por completo la modificación.  Tarea 11.14: Prueba de Aislamiento de Roles: Intentar consumir el endpoint de aprobación (PATCH /aprobar) usando un token de "Secretaría", garantizando que el sistema rechace la petición (solo Dirección puede aprobar).

Para la HU-12: Historial de auditoría documental , el objetivo principal es proporcionar transparencia y control contra posibles irregularidades. Esto permite que un administrador pueda consultar el log de cambios de cualquier trámite para verificar quién y cuándo realizó una modificación.  Con esta historia cerramos la Épica 1 (Módulo Documental). Como tu AGENTE FULLSTACK, he diseñado las siguientes tareas aplicando el patrón de diseño Event Sourcing para garantizar la inmutabilidad del historial:🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 12.1: Crear la tabla inmutable Auditoria_Tramites (o Historial_Tramites). Esta tabla debe contener las columnas: id_historial (PK), id_tramite (FK), id_usuario (FK, el administrador/secretaria que ejecutó la acción), fecha_hora (DATETIME), estado_anterior (VARCHAR) y estado_nuevo (VARCHAR).  Tarea 12.2: (Opcional según la arquitectura preferida) Implementar Triggers (Disparadores) DML de tipo AFTER UPDATE en la tabla principal Tramites. Esto asegura que cualquier cambio de estado inserte automáticamente un registro en la tabla de auditoría a nivel de base de datos, evitando que algún error en el backend omita el registro.Tarea 12.3: Crear la consulta SELECT que recupere el historial de un trámite específico, ordenado cronológicamente (ORDER BY fecha_hora DESC), uniendo (JOIN) la tabla de usuarios para obtener el nombre exacto de quien realizó la acción.  ⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Tarea 12.4: Desarrollar el endpoint HTTP GET /api/tramites/:id/auditoria.Tarea 12.5: Implementar el control de acceso (RBAC) más estricto del módulo. El middleware debe verificar el JWT y bloquear la petición con un 403 Forbidden si el usuario no tiene explícitamente el rol de "Administrador" o "Dirección".  Tarea 12.6: Asegurar que los servicios creados en las HU previas (HU-08, 09, 10, 11) emitan los eventos correctos hacia la capa de persistencia para que el registro de auditoría almacene el estado anterior y el nuevo estado con precisión.  💻 Capa de Presentación (Frontend: Híbrido Web/Móvil con Flutter)Tarea 12.7: Modificar el Modal de "Detalle del Trámite" (construido en la HU-08 y HU-11) para incluir una nueva pestaña o sección colapsable llamada "Trazabilidad".  Tarea 12.8: Implementar renderizado condicional (Conditional Rendering) en el frontend. La sección de "Trazabilidad" debe mantenerse oculta en la interfaz y ni siquiera renderizarse en el DOM/Árbol de Widgets si el usuario logueado es un Apoderado o una Secretaria.  Tarea 12.9: Diseñar la interfaz de la trazabilidad como una "Línea de Tiempo" (Timeline) visual, donde cada nodo represente un cambio de estado, mostrando claramente la Fecha/Hora, el Usuario responsable y la transición (Ej: De "Pendiente" a "Observado").  🔒 Pruebas de Calidad y Seguridad (QA)Tarea 12.10: Prueba de Escalada de Privilegios: Escribir un test E2E intentando consumir el endpoint de auditoría utilizando un token de "Secretaría", para garantizar que el sistema cumpla con la restricción de acceso exclusiva para Dirección/Administración.  Tarea 12.11: Prueba de Inmutabilidad: Verificar que la API no exponga bajo ninguna circunstancia endpoints de tipo PUT, PATCH o DELETE para la ruta /auditoria. Los logs deben ser estrictamente de solo anexado (append-only).

¡Excelente! Con la HU-13: Visualización del estado de cuenta consolidado, damos inicio a la Épica 2: Módulo de Tesorería, Morosidad y Automatización.  El objetivo principal de esta historia es que el apoderado pueda consultar su estado de cuenta detallado en Soles (S/), conociendo el monto exacto de sus obligaciones para evitar retrasos. Esta es una historia crítica de lectura (Read) que servirá de base para la automatización futura.  Como tu AGENTE FULLSTACK, aquí tienes el desglose técnico de tareas garantizando la precisión matemática y arquitectónica:🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 13.1: Diseñar las tablas financieras transaccionales: Conceptos_Pago (ej. Matrícula, Pensión Marzo, etc.) y Estado_Cuenta (o Deudas). Se debe aplicar estricta integridad referencial mediante Llaves Foráneas (FK) para impedir la existencia de estados financieros vinculados a alumnos no matriculados.  Tarea 13.2: Configurar los tipos de datos monetarios. Es mandatorio utilizar tipos precisos como DECIMAL(10,2) en SQL Server para evitar los errores de redondeo de punto flotante al consolidar saldos en Soles (S/).Tarea 13.3: Crear una consulta DML o Procedimiento Almacenado que ejecute un SUM() de las deudas activas, agrupadas y filtradas por el ID del apoderado, sumando las obligaciones de todos los alumnos que tiene asociados.  ⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Tarea 13.4: Desarrollar la lógica de negocio central en la capa de Dominio, creando el caso de uso CalculateDebtUseCase. Este módulo será responsable de orquestar la suma total y preparar la estructura de datos.Tarea 13.5: Construir el endpoint HTTP GET /api/tesoreria/estado-cuenta/me. El ID del apoderado solicitante debe extraerse de forma segura mediante el token JWT en el API Gateway, impidiendo que se pase por URL.Tarea 13.6: Estructurar la respuesta JSON del endpoint para que devuelva un objeto consolidado (el total general a pagar) y un arreglo detallado (desglose) que contenga cada concepto de pago, el monto individual y la fecha de vencimiento.  💻 Capa de Presentación (Frontend: Híbrido Web/Móvil con Flutter)Tarea 13.7: Maquetar el Dashboard Financiero del Apoderado. Diseñar una tarjeta principal (Hero Card) en la parte superior que muestre en fuente grande y destacada la Deuda Total Consolidada en S/.Tarea 13.8: Desarrollar una tabla o lista interactiva debajo del total que muestre el desglose detallado de la deuda por alumno. Cada fila debe mostrar claramente el nombre del estudiante, el concepto (ej. Pensión Abril), el monto en S/ y la fecha de vencimiento.  Tarea 13.9: Implementar renderizado condicional. Si el apoderado está al día con sus pagos (deuda = 0), la interfaz debe mostrar un Empty State positivo (ej. un icono verde de verificación con el mensaje "No presenta deudas pendientes").🔒 Pruebas de Calidad, Matemática y Seguridad (QA)Tarea 13.10: Pruebas de Aislamiento Financiero (Tenant Isolation): Ejecutar tests automatizados que garanticen matemáticamente que el token de un Apoderado A sea incapaz de consultar o sumar las deudas pertenecientes a la familia de un Apoderado B.Tarea 13.11: Pruebas de Precisión Matemática: Crear pruebas unitarias sobre el CalculateDebtUseCase en Node.js que validen escenarios con múltiples alumnos y cuotas parciales, asegurando que el total enviado al frontend concuerde al centavo.

Para la HU-14: Reporte y subida de comprobante de pago, el objetivo es digitalizar la recepción de pagos. El apoderado podrá reportar sus depósitos enviando una foto o PDF del voucher junto con los datos de la operación, cambiando el estado de su deuda a "En Validación".  Además, según la especificación arquitectónica base de este proyecto, esta historia introduce una integración clave con Inteligencia Artificial y validación humana (HITL - Human-in-the-Loop).Como tu AGENTE FULLSTACK, aquí tienes el desglose técnico estructurado:🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 14.1: Crear la tabla transaccional Reporte_Pagos (o Comprobantes). Debe incluir Llaves Foráneas (FK) vinculadas al id_estado_cuenta y al id_apoderado.Tarea 14.2: Definir las columnas necesarias: numero_operacion (VARCHAR), fecha_pago (DATE), url_voucher (VARCHAR, para la ruta de S3) y estado_validacion (con valor por defecto estricto de "En Validación").  Tarea 14.3: Crear un bloque de transacciones (Transact-SQL) que, al insertar el comprobante, actualice simultáneamente el campo estado en la tabla Estado_Cuenta (de "Pendiente" a "En Validación") para que el apoderado sepa que su pago está en proceso de revisión.  ⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Tarea 14.4: Desarrollar el endpoint POST /api/tesoreria/pagos/reportar habilitado para recibir multipart/form-data (archivos e inputs de texto simultáneamente).Tarea 14.5: Reutilizar el servicio de almacenamiento (S3 Upload Service) creado en la HU-06 para procesar el voucher adjunto, asegurando que cumpla con los límites de tamaño (ej. 5MB) y formato (PDF/JPG).  Tarea 14.6: Implementar validación de datos en el servidor: Verificar que la fecha_pago no sea una fecha futura y que el numero_operacion no se encuentre registrado previamente en otro comprobante aprobado (prevención de fraudes o doble uso de vouchers).🤖 Integración de IA y Arquitectura HITL (Human-in-the-Loop)Tarea 14.7: Configurar un flujo de fondo (Background Job / Webhook hacia n8n o un Agente Especialista). Al subir el voucher, el Agente con capacidades de Visión por Computadora (OCR) ejecutará un Tool Call para leer la imagen/PDF del comprobante.Tarea 14.8: Desarrollar la lógica de pre-conciliación: El Agente IA intentará extraer el "Monto" y el "Número de Operación" de la imagen, guardando estos datos pre-procesados en la base de datos para facilitar el trabajo del Tesorero en el siguiente paso (HU-16).💻 Capa de Presentación (Frontend: Híbrido Web/Móvil con Flutter)Tarea 14.9: Maquetar el formulario "Reportar Pago". Debe incluir un campo de texto para ingresar el Número de Operación, un componente Date Picker para seleccionar la Fecha del pago y un botón de File Picker para adjuntar el Voucher.  Tarea 14.10: Conectar el formulario al dashboard de estado de cuenta (HU-13). El apoderado debe seleccionar qué cuota específica (ej. "Pensión Abril") está pagando para que el sistema asocie el id_estado_cuenta correctamente.Tarea 14.11: Implementar retroalimentación visual reactiva. Al confirmarse el HTTP 201 Created, la UI debe cambiar el badge (insignia) de la cuota pagada a "En Validación" (color amarillo/naranja) inmediatamente, desactivando el botón de pago para esa cuota específica y evitando reenvíos.  🔒 Pruebas de Calidad y Seguridad (QA)Tarea 14.12: Prueba de Concurrencia: Simular múltiples intentos rápidos (spam de clics) de reportar un pago sobre la misma cuota, asegurando que la base de datos rechace los envíos duplicados.Tarea 14.13: Pruebas del Modelo IA: Subir imágenes borrosas o no relacionadas con vouchers bancarios para verificar que el Agente IA maneje el error correctamente sin interrumpir el flujo (dejando los campos en blanco para que el Tesorero los llene manualmente).

Para la HU-15: Dashboard de Tesorería para control de morosidad , el objetivo es proporcionar al tesorero una herramienta analítica (tablero de control) para monitorear visualmente los focos de morosidad, permitiendo filtrar la información por nivel, grado y sección. Esto es clave para la toma de decisiones sobre qué aulas requieren mayor atención.  Como tu AGENTE FULLSTACK, he estructurado las tareas necesarias para construir este dashboard priorizando el rendimiento de las consultas y la visualización de datos:🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 15.1: Crear índices compuestos (Composite Indexes) en las tablas de Alumnos y Matriculas sobre las columnas nivel, grado y seccion para optimizar drásticamente la velocidad de las consultas combinadas de lectura.Tarea 15.2: Construir una Vista de Base de Datos (VIEW) o una consulta de agregación DML utilizando SUM() y GROUP BY que consolide la deuda total agrupada por nivel educativo (Primaria, Secundaria, etc.) para alimentar los gráficos del dashboard.  Tarea 15.3: Desarrollar el Procedimiento Almacenado (Stored Procedure) que reciba los parámetros opcionales de búsqueda (Nivel, Aula, o Nombre del estudiante) y retorne el detalle de los alumnos morosos específicos que coincidan con los filtros.  ⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Tarea 15.4: Implementar el middleware de control de acceso (RBAC) para garantizar que los endpoints de este dashboard solo puedan ser consumidos por usuarios con el rol de "Tesorería" o "Administrador".Tarea 15.5: Desarrollar el endpoint GET /api/tesoreria/dashboard/consolidado que retorne la data agregada (deuda total por nivel)  para la renderización inicial rápida de la pantalla.  Tarea 15.6: Desarrollar el endpoint GET /api/tesoreria/dashboard/detalle que procese los parámetros de consulta (query params como ?nivel=Secundaria&aula=3A) y devuelva la lista paginada de estudiantes en morosidad.  Tarea 15.7: Crear el servicio y endpoint de exportación GET /api/tesoreria/dashboard/exportar. Este servicio debe utilizar librerías de generación de archivos (como exceljs para Excel o pdfkit para PDF) que tomen la vista filtrada actual y la devuelvan como un archivo descargable (Stream binario).  💻 Capa de Presentación (Frontend: Híbrido Web/Móvil con Flutter)Tarea 15.8: Maquetar el componente visual superior del Dashboard. Integrar una librería de gráficos (ej. fl_chart en Flutter o Chart.js en Web) para mostrar visualmente el consolidado de deuda total por nivel educativo (gráficos de barras o anillos).  Tarea 15.9: Construir la barra de herramientas de filtrado activo con menús desplegables (Dropdowns) dinámicos en cascada: al seleccionar un "Nivel", se habilitan los "Grados", y luego las "Secciones". Añadir también un campo de texto para búsqueda directa por nombre de estudiante.  Tarea 15.10: Implementar la tabla de datos interactiva (DataGrid) que muestre los resultados detallados (Nombre del alumno, Aula, Monto Adeudado en S/ y Días de mora).Tarea 15.11: Integrar un botón de "Exportar Resultados" que, al hacer clic, consuma el endpoint de exportación enviando los filtros actuales aplicados y dispare la descarga del archivo en formato PDF o Excel en el dispositivo del tesorero.  🔒 Pruebas de Calidad y Rendimiento (QA)Tarea 15.12: Pruebas de Carga (Load Testing): Validar que las consultas agregadas en SQL Server puedan procesar el consolidado de toda la institución en menos de 3 segundos (cumpliendo con el RNF-02), verificando que los índices compuestos de la Tarea 15.1 actúen correctamente.Tarea 15.13: Pruebas de Exportación: Asegurar que el archivo PDF/Excel generado respete estrictamente los filtros que el usuario haya seleccionado en pantalla y no exporte la base de datos completa por error.

Para la HU-16: Registro y conciliación de pagos, llegamos a un punto crítico del Módulo de Tesorería. El objetivo es que el tesorero valide o rechace los comprobantes de pago enviados por los apoderados, actualizando de forma oficial el estado financiero del alumno.  De acuerdo a la especificación técnica de tu proyecto, aquí convergen dos elementos clave: las propiedades ACID de la base de datos para evitar inconsistencias financieras, y el flujo HITL (Human-in-the-Loop) donde el humano supervisa la extracción previa de la Inteligencia Artificial.Como tu AGENTE FULLSTACK, aquí tienes el desglose detallado de tareas:🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 16.1: Crear un bloque de transacciones seguras (Transact-SQL con BEGIN TRAN, COMMIT y ROLLBACK) para la conciliación. Si el pago es aprobado, la base de datos debe descontar el monto del saldo pendiente en la tabla Estado_Cuenta y cambiar el estado_validacion a "Aprobado" en un solo movimiento atómico (cumplimiento estricto ACID).Tarea 16.2: Añadir una columna motivo_rechazo (VARCHAR) en la tabla de Reporte_Pagos (o equivalente) que se llenará obligatoriamente si el tesorero deniega el voucher.  Tarea 16.3: Aplicar el principio de "cero borrados físicos". Los pagos rechazados solo deben sufrir un borrado o anulación lógica (cambio de estado), pero el registro del intento debe permanecer en la base de datos para auditoría.  ⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Tarea 16.4: Desarrollar el endpoint HTTP PATCH /api/tesoreria/pagos/:id/validar. Este servicio ejecutará la transacción de la Tarea 16.1. Al confirmarse, el saldo del alumno se reduce automáticamente.  Tarea 16.5: Desarrollar el endpoint HTTP PATCH /api/tesoreria/pagos/:id/rechazar. Implementar validación en el middleware (ej. con Zod) para garantizar que el campo motivo venga incluido obligatoriamente en el cuerpo de la petición.  Tarea 16.6: Integrar reglas de Máquina de Estados: el servidor debe verificar que el pago se encuentre estrictamente en estado "En Validación". Si el pago ya fue aprobado o rechazado previamente, debe retornar un error 409 Conflict.Tarea 16.7: Disparar un evento de notificación (Webhook/SSE). Si el pago es rechazado, se debe notificar en tiempo real al apoderado (en su portal) para que vuelva a subir el archivo correcto.🤖 Integración de IA y Arquitectura HITL (Human-in-the-Loop)Tarea 16.8: Consolidar el flujo HITL. El backend debe enviar a la interfaz del tesorero los datos "pre-extraídos" por el Agente de Visión (HU-14). El tesorero actuará como el "Humano en el Bucle", limitándose a comparar visualmente que el "Monto" leído por la IA coincida exactamente con la imagen del voucher, acelerando el proceso de revisión.💻 Capa de Presentación (Frontend: Híbrido Web/Móvil con Flutter)Tarea 16.9: Maquetar la "Consola de Conciliación" dentro del Dashboard de Tesorería. Debe consistir en una vista dividida (Split View): a la izquierda, el visor del comprobante (imagen o PDF adjuntado por el usuario ); a la derecha, el panel con los datos de la transacción (N° de Operación, Monto sugerido por la IA y Fecha).  Tarea 16.10: Implementar los botones de decisión grandes y accesibles: "Validar" (Verde) y "Rechazar" (Rojo).  Tarea 16.11: Construir un Modal o Cuadro de Diálogo que se active al presionar "Rechazar". Debe contener un campo de texto obligatorio para ingresar el motivo de la denegación (ej. "Voucher borroso" o "Monto incompleto").  Tarea 16.12: Actualizar el estado de cuenta del padre (HU-13). Tras una validación exitosa, la UI del apoderado debe reflejar instantáneamente el nuevo saldo (reducción de deuda) y cambiar la etiqueta de la cuota a "Pagado".  🔒 Pruebas de Calidad, Auditoría y Seguridad (QA)Tarea 16.13: Pruebas de Integridad ACID: Forzar una interrupción en el servidor (simular caída de base de datos) a mitad de la validación de un pago, garantizando mediante tests automatizados que se ejecute el ROLLBACK y ni el estado de cuenta ni el recibo queden en estados inconsistentes.Tarea 16.14: Test de Rechazo: Validar que enviar la petición de rechazo al backend sin un texto explicativo devuelva un error del servidor, asegurando que el padre siempre reciba el motivo de su rechazo.  

Para la HU-17: Cálculo y actualización automática de morosidad, entramos al verdadero corazón del motor de automatización de tu Mini-ERP.  El objetivo de esta historia es que el sistema asuma la carga operativa de evaluar, de forma invisible y sin intervención humana, quiénes están al día y quiénes no. Esto eliminará por completo los errores de cálculo humano y asegurará la precisión financiera en S/.  Dado que es una historia netamente transaccional y de "procesos en segundo plano" (Background Jobs), esta vez no tendremos tareas de Frontend (la UI ya se construyó en la HU-13 y HU-15). Como tu AGENTE FULLSTACK, aquí tienes el desglose técnico orientado a la base de datos y la orquestación:🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 17.1: Desarrollar el Stored Procedure (Procedimiento Almacenado) principal, por ejemplo sp_ActualizarMorosidadDiaria.  Tarea 17.2: Escribir la lógica transaccional DML dentro del procedimiento. Este debe ejecutar un UPDATE masivo sobre la tabla Estado_Cuenta. Debe realizar una comparación matemática exacta entre la fecha_vencimiento de la cuota y la fecha_actual (GETDATE() en SQL Server).  Tarea 17.3: Calcular y mutar el campo dias_mora. Se utilizará la función DATEDIFF(day, fecha_vencimiento, GETDATE()) para obtener el número exacto de días de retraso.  Tarea 17.4: Mutar el campo estado_pago. Dentro del mismo bloque, si los días de mora son mayores a cero y el estado actual es "Pendiente", el sistema debe sobrescribir el valor de la columna a "Moroso".  ⚙️ Capa de Lógica de Negocio y Automatización (Backend / CRON)Tarea 17.5: Configurar el disparador (Trigger) de tiempo. Para cumplir estrictamente con el criterio de ejecución a las 00:00 horas, se debe configurar un Job en el SQL Server Agent que llame al Stored Procedure todos los días a la medianoche de la zona horaria del servidor (Lima/Perú).  Tarea 17.6: (Alternativa/Complemento desde Node.js): Si SQL Server Agent no está disponible en la capa de nube, implementar un proceso CRON en el backend de Node.js (usando librerías como node-cron) configurado con la expresión 0 0 * * * (todos los días a las 00:00) para que Node.js envíe la orden de ejecución a la base de datos de manera externa.Tarea 17.7: Implementar un registro de logs (Log de Ejecución). Cada vez que el CRON o Stored Procedure termine de ejecutarse, debe guardar un registro en una tabla del sistema indicando: Hora de inicio, Hora de fin, y la cantidad de filas (alumnos) que fueron pasadas a estado "Moroso", para monitoreo de los administradores.🔒 Pruebas de Calidad, Matemática y QATarea 17.8: Pruebas de Viaje en el Tiempo (Time Travel Testing): En el entorno de desarrollo (Staging), crear recibos de prueba con fechas de vencimiento de "ayer", "hoy" y "mañana". Forzar la ejecución del Stored Procedure y validar que solo las cuotas de "ayer" cambien a "Moroso" y calculen exactamente 1 día de mora.Tarea 17.9: Pruebas de Inmunidad: Asegurar mediante tests que el Stored Procedure no modifique bajo ninguna circunstancia el estado ni calcule días de mora a los recibos que ya poseen el estado "Pagado" o "En Validación".

Para la HU-18: Envío de alerta preventiva (5 días antes), entramos a la fase activa de comunicación automatizada del Mini-ERP.El objetivo de esta historia es enviar un recordatorio automático exactamente 5 días antes del vencimiento de la pensión, con un tono informativo y amable, para fomentar el pago voluntario y reducir el olvido. Aquí es donde el backend se conecta con el orquestador n8n.  Como tu AGENTE FULLSTACK, he desglosado las tareas técnicas necesarias abarcando la base de datos, el backend y el motor de flujos:🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 18.1: Crear una consulta DML (SELECT) o una Vista (VIEW) que filtre estrictamente a los alumnos cuya cuota tenga estado "Pendiente" y cuya fecha_vencimiento sea exactamente igual a GETDATE() + 5.  Tarea 18.2: Asegurar que la consulta aplique JOIN con la tabla de Apoderados para extraer el número de celular de contacto y el nombre del padre, así como el monto exacto de la cuota pendiente en S/.  ⚙️ Capa de Lógica de Negocio (Backend: Node.js)Tarea 18.3: Implementar un proceso programado (CRON job en Node.js o a través del SQL Server Agent) que se ejecute diariamente a una hora prudente (ej. 09:00 AM) para realizar la evaluación de los próximos vencimientos.Tarea 18.4: Desarrollar el servicio despachador de Webhooks (WebhookDispatcherService). Este servicio iterará sobre la lista de apoderados resultante de la consulta y estructurará un payload JSON seguro con los datos necesarios (Nombre Apoderado, Nombre Alumno, Monto en S/, Fecha de Vencimiento, y Celular).Tarea 18.5: Implementar lógica de reintentos (Retry Logic) y un patrón de Circuit Breaker. Si el endpoint de n8n está caído momentáneamente, el backend debe intentar reenviar el webhook un número limitado de veces antes de registrar un error, asegurando que las notificaciones no se pierdan.🤖 Capa de Automatización y Orquestación (n8n)Tarea 18.6: Crear un nuevo flujo de trabajo (Workflow) en n8n que inicie con un nodo de tipo "Webhook Trigger" configurado para recibir peticiones POST desde el backend de Node.js.  Tarea 18.7: Configurar el nodo de integración con la API de mensajería (ej. WhatsApp Business API o similar) dentro de n8n.  Tarea 18.8: Diseñar la plantilla del mensaje aplicando el framework de Prompting para que tenga un tono amable e informativo. El mensaje debe mapear las variables del JSON entrante (ej. "Hola {{nombre}}, le recordamos amablemente que el {{fecha_vencimiento}} vence la cuota de {{alumno}} por el monto de S/ {{monto}}.").  🔒 Pruebas de Calidad y QATarea 18.9: Prueba de Exactitud de Fechas: En el entorno de pruebas, modificar la fecha de vencimiento de una cuota para que venza en 4, 5 y 6 días. Validar que el CRON solo capture y dispare el webhook para el registro que vence exactamente en 5 días.  Tarea 18.10: Prueba de Carga del Webhook: Simular el envío de 500 alertas simultáneas al endpoint de n8n para verificar que la plataforma soporte la concurrencia y no sufra tiempos de espera excesivos (evitando Timeouts).

Para la HU-19: Envío de alerta de morosidad, pasamos de la prevención a la acción correctiva. El objetivo de esta historia es que el sistema envíe un recordatorio automático exactamente 3 días después de la fecha de vencimiento de la cuota, con la finalidad de acelerar la recuperación de la cartera vencida, poniendo especial foco en el nivel secundaria.  Como tu AGENTE FULLSTACK, he desglosado las tareas técnicas para esta historia crítica asegurando que la comunicación sea directa y precisa:🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 19.1: Desarrollar una consulta DML (SELECT) o un Procedimiento Almacenado que filtre estrictamente a los alumnos que mantengan deuda no cancelada pasada la fecha de vencimiento. Específicamente, la condición debe buscar donde la fecha_vencimiento sea igual a GETDATE() - 3 y el estado sea "Moroso".  Tarea 19.2: Construir los JOIN necesarios con las tablas de Matriculas (para asegurar que sea del nivel secundaria si se desea segmentar), Apoderados y Conceptos_Pago para recuperar el número de celular, nombre del padre, el concepto de la deuda y el monto total exacto expresado en Soles (S/).⚙️ Capa de Lógica de Negocio (Backend: Node.js)Tarea 19.3: Ampliar el proceso programado (CRON job) implementado en la HU-18 para que, además de buscar vencimientos futuros, evalúe las cuotas ya vencidas que cumplan los 3 días de retraso.Tarea 19.4: Implementar la orquestación del Webhook. El backend debe estructurar el payload (carga útil) en formato JSON con los datos del apoderado, el concepto adeudado y el monto, y disparar la petición HTTP POST hacia la URL del orquestador n8n.  🤖 Capa de Automatización y Orquestación (n8n)Tarea 19.5: Crear un flujo de trabajo (Workflow) paralelo o una ramificación (nodo Switch/If) en n8n que escuche el Webhook específico de "Alerta de Morosidad".Tarea 19.6: Configurar el nodo de mensajería (ej. WhatsApp). El mensaje redactado (Prompt) debe ser asertivo y directo, incluyendo explícitamente el concepto específico de la deuda y el monto total en S/ para no dejar lugar a dudas. Este aviso directo al dispositivo móvil del apoderado es el mecanismo principal para mitigar la morosidad.  🔒 Pruebas de Calidad y QATarea 19.7: Pruebas de Lógica de Fechas: Configurar registros de prueba con 1, 2, 3 y 4 días de mora. Verificar que el disparador solo procese y envíe el Webhook para el registro que tiene exactamente 3 días vencido, evitando enviar mensajes (Spam) todos los días al mismo padre.Tarea 19.8: Auditoría del Payload: Interceptar la petición saliente hacia n8n en el entorno de desarrollo y validar que el JSON contenga correctamente mapeados los campos de "concepto_deuda" y "monto_S/".

Para la HU-20: Registro de log de notificaciones , el objetivo es proporcionar al administrador una herramienta de auditoría para visualizar el historial de los mensajes enviados. Esto es vital para la resolución de incidencias, especialmente si un apoderado afirma no haber recibido su aviso de cobranza.  Como tu AGENTE FULLSTACK, he desglosado las tareas técnicas para asegurar que la trazabilidad de la integración con n8n quede perfectamente documentada:  🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)Tarea 20.1: Crear la tabla logs_notificaciones. Esta debe incluir obligatoriamente las columnas: id_log (PK), id_alumno (FK), fecha_envio (DATETIME) y estado_respuesta_api (VARCHAR/INT para almacenar el código HTTP retornado por n8n).  Tarea 20.2: Añadir índices (Indexes) sobre las columnas fecha_envio y id_alumno para optimizar las consultas futuras del administrador, ya que la interfaz deberá ser filtrable por fechas.  ⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)Tarea 20.3: Modificar el servicio despachador de Webhooks (creado en las HU-18 y HU-19). Tras cada disparo del webhook hacia n8n, el backend debe ejecutar un INSERT asíncrono en la tabla logs_notificaciones, capturando si la petición fue exitosa (ej. 200 OK) o si falló.  Tarea 20.4: Desarrollar el endpoint HTTP GET /api/admin/logs/notificaciones. Este servicio debe implementar control de acceso (RBAC) para asegurar que solo usuarios con rol de "Administrador" puedan consumirlo.  Tarea 20.5: Configurar los parámetros de consulta (query params) en el endpoint para aceptar un rango de fechas (ej. ?fecha_inicio=2026-05-01&fecha_fin=2026-05-31), permitiendo delegar el filtrado a la base de datos.  💻 Capa de Presentación (Frontend: Híbrido Web/Móvil con Flutter)Tarea 20.6: Maquetar la vista de "Auditoría de Notificaciones" en el panel del administrador. Se debe incluir una tabla de datos (DataGrid) que muestre el historial.  Tarea 20.7: Implementar componentes visuales para el filtrado por fechas (Date Range Picker)  y un buscador por ID del Alumno o nombre.  Tarea 20.8: Aplicar indicadores visuales (Badges) en la tabla para la columna "Estado de respuesta del API", mostrando verde si el webhook llegó correctamente a n8n, o rojo si hubo un error de comunicación.  🔒 Pruebas de Calidad y QATarea 20.9: Prueba de Registro de Fallos: Apagar temporalmente el flujo en n8n (o cambiar la URL del webhook en el entorno de pruebas) y disparar una alerta, verificando que el backend guarde correctamente el log automático  con el estado de error (ej. 500 o 404).  Tarea 20.10: Prueba de Filtrado: Insertar logs falsos con fechas de meses pasados y verificar que la interfaz de consulta filtrable por fechas  retorne estrictamente los registros correspondientes al rango seleccionado.  


Para la HU-21: Generación de reportes financieros en PDF, el objetivo es permitir que la Dirección obtenga consolidaciones de deudas en un formato profesional, facilitando la toma de decisiones estratégicas ante la promotora o juntas directivas.

Como tu AGENTE FULLSTACK, he desglosado las tareas técnicas necesarias, enfocándome en la arquitectura de generación dinámica de documentos:

🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)
Tarea 21.1: Construir una consulta SQL compleja o Procedimiento Almacenado que extraiga los datos financieros consolidados. Esta consulta debe realizar JOINs entre Estado_Cuenta, Alumnos, Niveles y Apoderados.

Tarea 21.2: Implementar filtros dinámicos en la consulta: el usuario debe poder filtrar por rango_fechas (ej. para reportes mensuales) o por nivel_educativo específico.

⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)
Tarea 21.3: Desarrollar el endpoint GET /api/tesoreria/reportes/financiero.

Tarea 21.4: Integrar una librería de generación de PDF en el servidor (ej. PDFKit, Puppeteer o jsPDF). Esta librería será responsable de inyectar los datos consultados en una plantilla predefinida.

Tarea 21.5: Implementar el diseño visual del reporte: incluir el logo institucional, la fecha de emisión, un encabezado profesional y la tabla de datos (Nombre del alumno, Monto en S/, Días de mora y Estado) formateada correctamente.

Tarea 21.6: Configurar la respuesta HTTP para que el servidor retorne un Stream Binario con el encabezado Content-Type: application/pdf. Esto permite que el navegador (o la App) descargue el archivo directamente sin necesidad de guardarlo físicamente en el servidor primero (optimizando recursos).

💻 Capa de Presentación (Frontend: Híbrido Web/Móvil con Flutter)
Tarea 21.7: Añadir en el Dashboard de Tesorería (HU-15) el botón "Exportar a PDF".

Tarea 21.8: Implementar la lógica para que el botón envíe los filtros seleccionados por el tesorero (ej. "Secundaria", "Mes de Mayo") como parámetros de consulta (query params) al backend.

Tarea 21.9: Implementar la descarga en el cliente. En Flutter (Web/Móvil), se debe capturar el flujo binario recibido del servidor y utilizar la librería path_provider o dart:html (si es Web) para guardar y abrir el archivo PDF resultante.

🔒 Pruebas de Calidad y QA
Tarea 21.10: Prueba de Integridad de Datos: Verificar que el total sumado en el reporte PDF coincida exactamente con la suma de las filas individuales del dashboard financiero, garantizando la consistencia contable.

Tarea 21.11: Pruebas de Formato: Validar que el PDF generado mantenga el logo institucional y que los montos en S/ estén correctamente formateados (con dos decimales) para evitar confusiones en la junta directiva.

Para la HU-22: Integración técnica y configuración de webhooks, abordamos la capa de conectividad que permite al Mini-ERP delegar procesos (como notificaciones) al orquestador n8n. Siguiendo los principios de Arquitectura Limpia (Clean Architecture), esta integración debe ser desacoplada y segura.  Como tu AGENTE FULLSTACK, he desglosado las tareas técnicas necesarias para establecer esta comunicación robusta:🛡️ Capa de Infraestructura y Seguridad (Shield)Tarea 22.1: Implementar un Middleware de Seguridad. Dado que estamos integrando dos sistemas (ERP y n8n), la comunicación nunca debe ser abierta. Se debe configurar un Secret Key o utilizar autenticación basada en Bearer Token que el backend del ERP incluya en el encabezado Authorization de cada solicitud POST enviada a n8n.  Tarea 22.2: Configurar el API Gateway o el servidor intermedio para actuar como un "Escudo Protector", garantizando que el flujo de datos sea unidireccional y restringido únicamente a los endpoints de n8n permitidos.  ⚙️ Capa de Lógica de Negocio (Backend: Event Dispatcher)Tarea 22.3: Crear un EventDispatcher en el backend. Siguiendo el principio de desacoplamiento, el servicio de tesorería o documental no debe llamar directamente a n8n. Debe publicar un evento en el bus interno (o simplemente encolar una tarea asíncrona) que el EventDispatcher consumirá.  Tarea 22.4: Implementar la Idempotencia en sistemas distribuidos. Dado que la red es intrínsecamente inestable, el servicio despachador debe implementar un mecanismo de Retry con Exponential Backoff (reintentos con espera creciente) para asegurar que, ante un fallo temporal, el mensaje llegue a su destino sin generar duplicados indebidos.  Tarea 22.5: Normalizar el esquema de datos (Payload). La IA y los flujos agénticos operan eficientemente con formatos modernos como JSON. Asegurar que el contrato de datos sea consistente: el ERP enviará un JSON estructurado con los objetos transaccionales (Contact, Event, Datos_Pago) que n8n pueda procesar inmediatamente.  🤖 Capa de Automatización (Orquestación en n8n)Tarea 22.6: Configurar el nodo "Webhook" en n8n como receptor. Este nodo debe estar configurado para recibir peticiones POST y validar el token de seguridad enviado por el ERP en el paso 22.1.Tarea 22.7: Definir la lógica de "Function Calling" o enrutamiento. n8n debe ser capaz de interpretar el JSON recibido para decidir qué agente especialista debe actuar (ej. si el payload indica "pago_rechazado", delegar al agente de facturación; si es "pago_pendiente", delegar al agente de recordatorios).  🔒 Pruebas de Calidad y QATarea 22.8: Prueba de Cero Confianza (Zero Trust): Intentar realizar un POST al webhook de n8n desde una terminal externa sin el token de autenticación. El sistema debe rechazar la conexión inmediatamente.  Tarea 22.9: Prueba de Resiliencia (Circuit Breaker): Simular una caída del servicio de n8n o del servicio de mensajería (WhatsApp/Email). Validar que el sistema de reintentos del ERP capture el error y que no se pierda la información transaccional (se mantenga en la cola o log de errores).  

Para la HU-23: Configuración del mensaje de alerta, el objetivo es otorgar autonomía a la administración para personalizar el tono y contenido de las comunicaciones sin requerir despliegues de código. Esto es fundamental para que el colegio pueda ajustar su comunicación (ej. ser más conciliadores o más formales) según la temporada académica.

Como tu AGENTE FULLSTACK, he desglosado las tareas técnicas para implementar este "Motor de Plantillas Dinámicas" de forma segura y eficiente:

🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)
Tarea 23.1: Crear la tabla Configuracion_Notificaciones. Esta tabla debe almacenar las plantillas base: id_config (PK), tipo_alerta (VARCHAR - ej: 'PREVENTIVA', 'MOROSIDAD'), cuerpo_mensaje (NVARCHAR(MAX)) y fecha_ultima_actualizacion (DATETIME).

Tarea 23.2: Crear un registro semilla (Seed) con los textos predeterminados, asegurando que utilicen los marcadores de posición (placeholders) estándar definidos, como {nombre_apoderado}, {nombre_alumno}, {monto_deuda} y {fecha_vencimiento}.

⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)
Tarea 23.3: Desarrollar los endpoints GET /api/admin/config/mensajes (para cargar la configuración actual en la UI) y PATCH /api/admin/config/mensajes (para guardar cambios).

Tarea 23.4: Implementar el Servicio de Interpolación de Strings. Esta es la lógica central: una función utilitaria que tome la plantilla de la base de datos y un objeto de datos (data payload) y reemplace los marcadores {variable} por sus valores reales de forma segura.

Tarea 23.5: Añadir validación de integridad: El sistema debe verificar antes de guardar la plantilla que esta contenga todos los marcadores necesarios. Si un usuario intenta guardar un mensaje que borra la variable {monto_deuda} (crítica para el cobro), el backend debe lanzar un error preventivo para evitar fallos en la comunicación futura.

💻 Capa de Presentación (Frontend: Híbrido Web/Móvil con Flutter)
Tarea 23.6: Maquetar la vista "Configuración de Notificaciones" en el Panel de Administrador. Debe incluir dos cajas de texto grandes (TextAreas) etiquetadas claramente: "Mensaje de Alerta Preventiva" y "Mensaje de Morosidad".

Tarea 23.7: Implementar una Guía de Variables visible en la pantalla (una pequeña lista o leyenda) que explique al administrador qué marcadores puede usar (ej. "Use {nombre_apoderado} para insertar el nombre del padre").

Tarea 23.8: Construir la función de Previsualización en Vivo. A medida que el administrador escribe en el TextArea, la pantalla debe renderizar un ejemplo ficticio del mensaje final (ej. reemplazando las variables con nombres de prueba como "Juan Pérez") para que el usuario vea cómo quedará el mensaje antes de guardar.

🔒 Pruebas de Calidad, Seguridad y Validación (QA)
Tarea 23.9: Prueba de Seguridad (Inyección): Validar que el campo de texto del administrador sea sanitizado para evitar que alguien intente inyectar etiquetas HTML o scripts maliciosos en la plantilla de WhatsApp.

Tarea 23.10: Prueba de Interpolación: Verificar que el sistema maneje correctamente los casos donde una variable es nula (ej. si falta el nombre del alumno, el sistema debe mostrar un valor por defecto seguro en lugar de romper el mensaje con un "undefined").


Para la HU-24: Auditoría de movimientos financieros, estamos cerrando el ciclo de seguridad y transparencia de todo el Mini-ERP. Esta historia es la "última línea de defensa" contra posibles malas prácticas, asegurando que cada ajuste manual realizado a los saldos de los alumnos quede registrado de forma indeleble.

Como tu AGENTE FULLSTACK, he desglosado las tareas técnicas para implementar este registro de auditoría inmutable:

🗄️ Capa de Persistencia (Base de Datos: Microsoft SQL Server)
Tarea 24.1: Crear la tabla Auditoria_Financiera. Esta tabla debe ser configurada como Append-Only (solo lectura y adición). Columnas esenciales: id_log (PK), id_deuda (FK), id_usuario (el administrador que ajustó la deuda), fecha_hora (DATETIME), monto_original (DECIMAL), monto_final (DECIMAL) y motivo_ajuste (NVARCHAR(MAX)).

Tarea 24.2: Implementar un Trigger DML de tipo AFTER UPDATE sobre la tabla Estado_Cuenta. Este disparador es automático: cada vez que alguien modifique un monto (incluso un administrador), el trigger capturará los valores antes y después del cambio e insertará el registro en Auditoria_Financiera sin que el código de la aplicación deba gestionarlo manualmente.

Tarea 24.3: Configurar los permisos de base de datos para que incluso el usuario administrador de la aplicación no tenga privilegios de DELETE o UPDATE sobre la tabla Auditoria_Financiera. La inmutabilidad se garantiza a nivel de motor de base de datos.

⚙️ Capa de Lógica de Negocio (Backend: Node.js con Express)
Tarea 24.4: Desarrollar el endpoint GET /api/tesoreria/auditoria.

Tarea 24.5: Implementar el control de acceso más restrictivo. Este endpoint debe validar mediante un middleware de seguridad que el usuario logueado tenga exclusivamente el rol de "Administrador" o "Dirección". Cualquier otro rol debe recibir un 403 Forbidden.

Tarea 24.6: Desarrollar la lógica de consulta para el historial. El backend debe permitir filtrar por id_alumno o fecha_rango, delegando el procesamiento pesado a la base de datos para mantener la latencia baja.

💻 Capa de Presentación (Frontend: Híbrido Web/Móvil con Flutter)
Tarea 24.7: Maquetar el módulo de "Auditoría Financiera" dentro del Dashboard de Tesorería o Administración. Esta vista debe presentar una tabla clara que muestre el "Quién, Cuándo y Qué" de cada ajuste.

Tarea 24.8: Diseñar un sistema de "Resaltado de Anomalías". Si el cambio de monto es significativo (ej. una reducción mayor al 20%), la fila en la tabla debe resaltarse visualmente (ej. en color amarillo o rojo) para facilitar la revisión visual rápida del director.

Tarea 24.9: Implementar filtros de búsqueda rápidos por "Nombre de Alumno" o "ID de Deuda" para que, ante el reclamo de un apoderado, el administrador pueda localizar instantáneamente el historial de cambios de ese saldo.

🔒 Pruebas de Calidad, Auditoría y Seguridad (QA)
Tarea 24.10: Prueba de Inmutabilidad: Intentar ejecutar un UPDATE manual sobre un registro existente en Auditoria_Financiera a través de la consola de SQL. Si la configuración de permisos (Tarea 24.3) es correcta, el motor debe rechazar la operación, demostrando que el log es a prueba de manipulaciones.

Tarea 24.11: Prueba de Trazabilidad: Realizar un ajuste de deuda a través del sistema, verificar que la tabla Estado_Cuenta cambie y, acto seguido, consultar el endpoint /api/tesoreria/auditoria para validar que el registro del ajuste aparezca correctamente con todos los campos (monto original, nuevo, motivo y usuario).